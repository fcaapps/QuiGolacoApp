import 'package:flutter/material.dart';

class AssistenciasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(
        child: Text(
          'Assistências Page',
          style: TextStyle(fontFamily: 'Roboto', fontSize: 30),
        ),
      ),
    );
  }
}
